/**
 * Import the theme constants related to colors and fonts from the 'theme.js' file.
 */
import { COLOR, FONT } from "./theme";

/**
 * Import the error constants containing error codes and their corresponding messages
 * from the 'error.js' file.
 */
import { ERROR } from "./error";

/**
 * Import the SVG image constants from the 'images.js' file.
 */
import { IMAGES } from "./images";

/**
 * Import the SVG icon constants from the 'icons.js' file.
 */
import { ICONS } from "./icons";

import { SPLOADER, UPLOADER } from "./loaders";

/**
 * Export all the imported constants so that they can be used in other modules.
 * The exported constants include COLOR, FONT, ERROR, IMAGES, and ICONS.
 */
export { COLOR, FONT, ERROR, IMAGES, ICONS, SPLOADER, UPLOADER };